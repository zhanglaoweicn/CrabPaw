# CrabPaw 项目整体检查报告（D1–D12，无死角）

- **项目**：CrabPaw (v2.2.0) — Electron+React GUI + Node 后端
- **检查日期**：2026-08-28
- **性质**：只读/隔离检查；含静态扫描、运行时探测、测试实跑、依赖审计、子代理深审
- **范围**：`src/`（后端）、`gui/`（前端）、`evals/`、`data/`、`docs/`、`config.yaml`、`.env.example`、CI、依赖清单

---

## 一、总体结论（按 D1–D12）

| 维度 | 状态 | 关键结论 |
|---|---|---|
| D1 代码静态 | 🟡 | eslint **0 error / 57 warning**（全 `no-unused-vars` 死代码/卫生） |
| D2 前后端对齐 | 🟡 | 端点存在性 ✅ 契约 5/5 + 运行时 38 请求全 200；但含 1 个 P0 崩溃 + 若干业务逻辑错位 |
| D3 运行时功能 | 🟢 | 后端健康（uptime ~23.5h）、公开端点正常、鉴权正确（无 token → 401） |
| D4 业务逻辑 | 🟡 | 会议转写排序、CollabOrbit 超时、面板状态机等缺陷 |
| D5 HARNESS 治理 | 🔴 | 子代理契约名字错位 + 评估门禁为红（417/418）+ 文档数字全面失真 |
| D6 安全 | 🟡 | 无越权；但命令注入/SQL 写绕过/钩子仅限 Bash/密钥脱敏不全/7 个 npm 漏洞 |
| D7 性能可靠性 | 🟢 | metrics 管道/健康监控在；SSE replayRing 有上限（无泄漏）；heap 463MB 观察 |
| D8 数据存储 | 🟡 | SQLite 正常；`data/.crabpaw/.crabpaw/` 双嵌套；密钥未入库 ✅ |
| D9 测试/CI | 🟡 | jest **1975/1977**（唯一套件缺 openssl）；eval **417/418**（门禁红） |
| D10 依赖/构建 | 🟡 | **7 个 npm 漏洞**（5 high/2 moderate）；gui typecheck ✅；kws-model 路径正确 |
| D11 文档一致 | 🔴 | 数字/方法名/行号/版本全面过期且互相矛盾 |
| D12 配置 | 🟡 | config.yaml 可解析；**86 环境变量 vs .env.example 约 18 个**（文档化缺口） |

**一句话**：功能主体健康（无空壳、无越权、无密钥入库、测试大体通过），但存在 **1 个前端崩溃级 bug、1 个破坏中的评估门禁、7 个依赖漏洞、若干业务逻辑/状态机缺陷、以及文档与实现的系统性失真**。

---

## 二、分维度详情

### D1 代码静态质量
- eslint `src/`：**0 errors**，**57 warnings（全部 `no-unused-vars`）**。未使用变量/导入集中在 prompt 构建类（`buildSkillsPrompt`、`buildSystemPrompt`、`buildLayeredSystemPrompt`、`loadSmartContext` 等）与若干常量（`PROMPT_LAST_MODIFIED` 等）——死代码/卫生项。
- 已知死代码：`buildCompactPrompt`（system-prompt.js:1342，仅导出无消费）、`createTrajectoryHooks`（harness-hooks.js:359，未注册）、`createDefaultMiddlewareChain`（middleware/index.js:125，无消费）、轨迹 `cleanup(30)`（未被调度）。
- 契约命名：24 个非 PascalCase 键，其中 `html-presentation`、`presentation-builder`（tool-contract.js:693/699）为真违规。
- **建议**：清理未使用变量（57 处）；删除/接线死代码。

### D2 前后端对齐
- **正向**：`frontend-routes.contract.test.js` **5/5 通过**（所有 `api*` 调用解析到后端路由）；运行时加载 GUI+管理舱 **38 请求全 200**；SSE/WS 端点（`/voice/cloud`、`/scene`、`/events`）均有后端支撑；SSE 事件名前后端一致。
- **P0 崩溃**：CommodityPanel（index.tsx:60-69）`setResult(res)` 后读顶层 `res.items/rows`，后端 `commodity.js:17/27` 返回扁平 `{success,...result}`，`api.ts requestFetch` 装进 `res.data` → `sortItems(undefined)` 抛 TypeError（崩溃）+ 历史恒空。修复：读 `res.data.*` 或后端归一化。
- **业务逻辑**：会议转写分段字典序排序（>9 乱序）、CollabOrbit 超时（`timeout` 落入 else → 永久 running 卡）、TaskPanelHost 订阅 `tool_call`（后端不广播）、schedule/knowledge 打开不写 `'open'`、typhoon 键未注册、`GET /panels/{stock,typhoon}` 绕过 open 同步。
- **低危**：TTS 前端 `_refined` vs 后端 `body.refine`（默认值对齐不失效）；useChatStream 若干。

### D3 运行时功能
- `/health`：`status:ok`，uptime ~23.5h，heapUsed **463MB**（偏高观察），database/config ok。
- `/status`：`success:true`，node v24.13.0。
- `/api/memory/stats` 无 token → **401**（鉴权正确生效）。
- 主功能可用（对话/管理舱/面板在运行时均 200）。

### D4 业务逻辑
- 见 D2 业务逻辑清单 + 设置「保存→reload→生效」链路自洽；记忆/文件生成/会议状态机自洽。
- 待修：CommodityPanel、会议转写排序、CollabOrbit timeout、面板 open/close 对称性、task-panel-store 忽略 stepId。

### D5 HARNESS 架构治理
- 8 维度实现均接线（无空壳）；但：
  - **子代理契约名（coder/researcher/…）≠ 实际 spawn 类型（research/implement/…）** → 对真实委派强校验失效。
  - **评估门禁为红**：`node evals/index.js` = **99.8% (417/418)**，唯一失败 `collab_009`（expert-collaboration）；CI eval 会红。
  - 文档数字全面失真：契约 231/207/209/22（文档 205/208/235/27）、评估 52/418（文档 53/22/15）、技能 57+20（文档 68/257）、审计 43（文档 42）。
  - 死代码组件 5 个。
- 详见 `docs/HARNESS_审计报告.md`。

### D6 安全（专项）
- **鉴权/越权**：无越权。`PUBLIC_ROUTES_BY_METHOD` 仅 GET 只读 + webhook；`/api/*` 全部非公开 → 401；`/files/` 有回环+token+路径防穿越二次校验。
- **信息泄露（低危）**：`/events` SSE 公开无 token，本机进程可订阅全部活动事件（secret 已脱敏，正文/路径未脱敏）。
- **命令注入（中危）**：Bash（bash-tools.js:263）、ShellExec（persistent-shell-tool.js:55-71）对不可信 command 直接执行，未统一白名单。
- **SQL 写绕过（中危）**：DatabaseQuery 仅查 `startsWith` 前缀（SELECT/WITH/…），`WITH x AS(...) DELETE FROM t` 可绕过；MySQL/PG 未设只读会话；未参数化。
- **SSRF（低-中危）**：skill-market 校验较完善但存在 DNS-rebinding TOCTOU 间隙；proxy-audio 跟随远端未校验重定向目标。
- **危险命令钩子（中危）**：`createSafetyHooks` 仅拦 Bash/bash（harness-hooks.js:321），**不覆盖 ShellExec**；黑名单用 `includes` 子串（`${IFS}`/base64 管道可绕过）；默认 command profile = `allow:['*']` 全放行。
- **密钥（低-中危）**：`redactSensitive` 仅按键名脱敏、非全覆盖；SetupWizard 空 apiKey 落 `'none'`；**`.gitignore` 有效——`.env`/`.api_keys.json`/`secrets.json`/`.api_token`/数据库均未被 git 追踪**（✅ 无密钥入库）；但工作区 `.env` 含真实格式 `BAIDU_API_KEY=…`（建议确认并轮换）。
- **文件权限（中危）**：路径钩子 `Read` 大小写不匹配（工具为 `Read`）；DEFAULT 模式读无限制不收紧工作区；PLAN 只读语义被 Bash/ShellExec 绕过；Windows 大小写不敏感 vs `startsWith` case-sensitive 可能失配。
- **CORS（低-中危）**：`Origin:null`（sandbox iframe）回 `Access-Control-Allow-Origin:*` 并放行授权头，放大公开端点跨域读取。
- **正向**：webhook 签名校验（飞书/企微，timingSafeEqual）✅；OAuth state 校验 ✅；secret 脱敏尽力而为。
- **依赖漏洞（D10 交叉）**：见下。

### D7 性能与可靠性
- metrics-pipeline（core，319L）：`getSnapshot` 存在、**`getStats` 不存在**（印证 HARNESS §9.4 文档错误）；cost 主要在 perception/usage-stats（core 版仅注释提及）。
- health-monitor：`checkIn`/`getHistory` 存在 ✅。
- **SSE 无泄漏**：`replayRing` 有上限（`REPLAY_MAX` shift）、`sseClients` 有清理 ✅。
- 内存：后端 heapUsed **463MB**（uptime ~23.5h），建议长期观察是否泄漏（无即时结论）。
- 超时/自愈：`ApiTimeoutError` abort 语义在 api 层贯穿；恢复链 watchdog 存在。

### D8 数据与存储
- SQLite：`unified-memory.db`（主记忆）、`fts-memory.db`、`skill-quality.db`（+wal/shm）存在于 `data/.crabpaw/`。
- **路径双嵌套确认**：`data/.crabpaw/.crabpaw/`（HARNESS §6.1 描述路径比实际少一层 `.crabpaw`）。
- 审计文件：`data/.crabpaw/audit-log.json`（JSONL）。
- **密钥未入库 ✅**：`data/` 下仅 3 个无敏感 JSON 被 git 追踪；`.crabpaw/` 下 `.env`/`.api_keys.json`/`secrets.json`/`.api_token` 均被 `.gitignore` 排除。
- 凭据以明文文件存于数据目录（`.api_token`、`secrets.json`），属本地应用正常但建议加密/权限收紧。
- 已知：轨迹 30 天清理未被调度（无限增长视角）；audit retention 非 30 天。

### D9 测试覆盖 / CI
- jest（`src/` + `test/`）：**162 套件 / 1977 用例，1975 通过（99.9%）**；唯一失败套件 `plugin-trust-x509`，原因是**缺 openssl 环境**（`Command failed: openssl req -x509`），非代码逻辑。
- 测试文件数：约 136（src/test、test、evals/test-cases）。
- eval：`node evals/index.js` = **417/418（collab_009 失败）**，门禁当前为红。
- CI：`.github/workflows/ci.yml` = lint（18/20/22 矩阵）→ test（needs lint, coverage）→ eval（needs test, node20 + regression v3），可执行；`npm run eval` 任一失败即退出码 1 阻塞合并。
- 覆盖率：CI 配了 coverage 上传，但关键模块（ai.js/panel/scene/task）未设硬阈值。

### D10 依赖与构建
- **npm audit：0 critical / 5 high / 2 moderate = 7 个漏洞**：
  - `extract-zip` HIGH（symlink 路径穿越，CWE-22，CVSS 8.1，**无修复**）——用于 skill 安装/解压。
  - `adm-zip` HIGH（畸形 ZIP 4GB 内存分配 DoS，修复 0.6.0）。
  - `js-yaml` HIGH（CVE-2026-59870 `!!omap` 二次 CPU 消耗；修复 3.15.1/4.3.1）——用于 config/skill 解析。
  - `image-size` HIGH（ICNS/JXL/HEIF 无限循环 DoS，经 pptxgenjs）。
  - `pptxgenjs` HIGH（经 image-size）。
  - `exceljs` MODERATE（经 uuid<11.1.1 越界）；`uuid` MODERATE。
- gui `npm run typecheck`：**EXIT 0**（通过）。
- kws-model 路径：`resources/kws-model` 正确（与 electron-builder extraResources 一致）；node.exe/data/sherpa 同理。
- package-lock 存在（root + gui）。
- 注：electron-builder 全量打包未在本轮执行（重负载）；gui 有独立 package-lock，建议单独 `npm audit`。

### D11 文档一致性
- 数字全面过期且互相矛盾：契约（205/208/235/27 vs 231/207/209/22）、评估（53/22/15 套、424/194 例 vs 52/418）、技能（68/257 vs 57+20）、审计（42 vs 43）。
- 方法名/行号错误：HARNESS §9.4 `getStats()` 应为 `getSnapshot()`；§9.2 `watermark>0.7` 失实；§9.1 行号偏移 80–130 行。
- §5.1 CI「lint→eval」漏 test；§8 版本表乱序（3.0.0 早于 2.4.0 却排后）；§6.1 轨迹路径少一层 `.crabpaw`；§11 行数（232L/177L→320/198）。
- `PROMPT_LAST_MODIFIED='2026-07-14'` 与文档宣称更新时间不符。
- 详见 `docs/HARNESS_审计报告.md`。

### D12 配置与发布
- `config.yaml` 解析 OK（system/models/tts/voiceReply/memory/evolution/tools/skills/skillRegistries/plugins/platforms/testing/logging/browser/monitoring）。
- **环境变量文档化缺口**：代码读取 **86 个环境变量**，`.env.example` 仅文档化约 18 个（`ADMIN_API_KEY`、`API_SERVER_*`、`FALLBACK_*`、`CRABPAW_*`、`WECOM_SEND_PORT`、`SECRET`、`CRABPAW_PII_SALT` 等均未记录）。
- `.gitignore` 覆盖完善（排除 `.env`、`data/.crabpaw/`、`data/skills/`、`data/workspace/`、`CrabPaw-Data/` 等，保留 `!.env.example`）。
- `DATA_DIR=./data/.crabpaw` → 印证轨迹双嵌套。

---

## 三、优先级修复清单

| 优先级 | 事项 | 位置 |
|---|---|---|
| 🔴 P0 | CommodityPanel 响应读取（崩溃） | CommodityPanel/index.tsx:60-69 |
| 🔴 P0 | 依赖漏洞：extract-zip（无补丁）/js-yaml/adm-zip | npm audit |
| 🔴 P0 | 评估门禁转绿（collab_009） | evals/test-cases/expert-collaboration.js |
| 🟠 P1 | 命令注入（Bash/ShellExec 白名单）；SQL 写绕过（DatabaseQuery 参数化+只读）；危险钩子覆盖 ShellExec | 安全 |
| 🟠 P1 | 子代理契约名字错位（registerDefaults vs SUBAGENT_TYPES） | agent-contract-validator.js |
| 🟠 P1 | 业务逻辑：会议转写排序/`getFullTranscript` 死逻辑、CollabOrbit timeout 收口、TaskPanelHost 订阅名、面板 open/close 对称、typhoon 键注册 | 前端 |
| 🟡 P2 | 文档一致性订正（HARNESS/README/AGENTS 数字、§9.4/§9.2/§5/§8/§6.1/§11） | docs |
| 🟡 P2 | 死代码清理（buildCompactPrompt、createTrajectoryHooks、createDefaultMiddlewareChain、轨迹 cleanup、57 处 no-unused-vars） | 全局 |
| 🟡 P2 | .env.example 补全环境变量（86 个） | 配置 |
| 🟢 P3 | 轨迹/audit 保留策略调度、Windows 大小写路径、CORS null→*、密钥脱敏全字段、SetupWizard 'none'、kebab-case 契约命名 | 加固 |

---

## 四、执行说明

- 方法：静态扫描（eslint/tsc/死代码）、运行时探测（/health、/status、鉴权 401）、测试实跑（jest 全量、frontend-routes 契约、npm audit）、多子代理分维度深审。
- 只读/隔离：未修改源码（仅先前修复的 `gui/src/components/CostDashboard/index.tsx` 与新增 docs）；未执行副作用命令。
- **环境依赖失败**：`plugin-trust-x509` 因缺 openssl 失败（本地工具缺失，非代码逻辑）。
- 4 个重头子代理（安全/构建/性能/数据）为长任务，安全（D6）与构建（D10）已产报告；性能（D7）/数据（D8）由直接核验覆盖。

---

*本报告为 CrabPaw 整体检查（D1–D12）综合结论；所有数字以当前工作区源码为准确实测所得。*
