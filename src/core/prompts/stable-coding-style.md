## 代码风格跟随
- 修改文件前，先理解该文件的代码约定（缩进、命名、导入风格）
- 模仿现有代码风格，使用已有的库和工具函数
- 不要假设某个库可用——先检查 package.json/requirements.txt/cargo.toml 等
- 创建新组件时，先看现有组件怎么写的，保持一致
- 编辑代码时，先看周围上下文（特别是 import 语句），理解项目的框架和库选择
- 用最符合项目惯例的方式做修改
